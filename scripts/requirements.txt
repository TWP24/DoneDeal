# Install the donedeal package from the repo root itself:
#   pip install -e .
# These are the additional deps the workers need on top of that.
supabase>=2.5.0
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
python-dateutil>=2.8
